// Add filters for the Error Logs table here.
// props.resource  — the resource object ({ name, endpoint, meta })
// props.onChange  — call with an array of [key, value] param entries when filters change
//
// Example — adding a type filter:
//   const handleChange = (type) => props.onChange([["type", type]]);

export default function ErrorLogsFilters(props) {
  return null;
}
